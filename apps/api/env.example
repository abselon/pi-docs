# =============================================================================
# API SERVER CONFIGURATION
# =============================================================================

# Port number for the API server
# Default: 3001
PORT=3001

# Node environment (development, test, or production)
# Default: development
NODE_ENV=development

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =============================================================================

# PostgreSQL database connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# 
# Examples:
#   Local PostgreSQL: postgresql://postgres:yourpassword@localhost:5432/pidocs_db
#   Supabase: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
#   Railway: postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/railway
#   Neon: postgresql://[USER]:[PASSWORD]@[HOST]/[DATABASE]?sslmode=require
#   AWS RDS: postgresql://[USER]:[PASSWORD]@[ENDPOINT]:5432/[DATABASE]
#
# IMPORTANT: Replace all placeholders with your actual database credentials
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/pidocs_db

# =============================================================================
# AUTHENTICATION & SECURITY (REQUIRED)
# =============================================================================

# JWT secret key for signing authentication tokens
# MUST be changed in production! Use a strong, random string (at least 32 characters)
# Generate one with: openssl rand -base64 32
# Default (dev only): dev_change_me
JWT_SECRET=dev_change_me

# Allowed CORS origins (comma-separated list of URLs)
# Add your frontend URLs here, e.g.:
#   Development: http://localhost:3000
#   Production: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost:3000

# Frontend URL (used for email verification and password reset links)
# Default: http://localhost:3000
FRONTEND_URL=http://localhost:3000

# =============================================================================
# FILE STORAGE CONFIGURATION
# =============================================================================

# Local file storage directory (used when storageProvider is LOCAL)
# Files will be stored in this directory relative to the API root
# Default: ./uploads
UPLOAD_DIR=./uploads

# =============================================================================
# AWS S3 STORAGE (OPTIONAL - Only needed if using S3 storage)
# =============================================================================

# AWS S3 region (e.g., us-east-1, eu-west-1)
# Leave empty if not using S3
S3_REGION=

# AWS S3 bucket name
# Leave empty if not using S3
S3_BUCKET=

# S3 key prefix for all uploaded files (organizes files in bucket)
# Default: pi-docs
S3_PREFIX=pi-docs

# AWS KMS Key ID for server-side encryption (optional)
# If empty, uses SSE-S3 (AES256) encryption
# Format: arn:aws:kms:region:account-id:key/key-id
S3_KMS_KEY_ID=

# =============================================================================
# EMAIL CONFIGURATION (OPTIONAL - Required for email verification & password reset)
# =============================================================================

# SMTP server hostname
# Examples:
#   Gmail: smtp.gmail.com
#   Outlook: smtp-mail.outlook.com
#   SendGrid: smtp.sendgrid.net
#   Mailgun: smtp.mailgun.org
#   AWS SES: email-smtp.[region].amazonaws.com
# Leave empty to log emails to console (development only)
SMTP_HOST=

# SMTP server port
# Common ports: 587 (TLS), 465 (SSL), 25 (unencrypted - not recommended)
# Default: 587
SMTP_PORT=587

# Use SSL/TLS encryption (true for port 465, false for port 587 with STARTTLS)
# Default: false
SMTP_SECURE=false

# SMTP authentication username (usually your email address)
# For Gmail: your-email@gmail.com
# For SendGrid: apikey
SMTP_USER=

# SMTP authentication password
# For Gmail: Use an "App Password" (not your regular password)
#   - Go to Google Account > Security > 2-Step Verification > App passwords
# For SendGrid: Your API key
# For AWS SES: Your SMTP password (different from AWS access key)
SMTP_PASS=

# From email address (sender address for all emails)
# Must be a valid email address
# Default: noreply@pi-docs.com
SMTP_FROM=noreply@pi-docs.com